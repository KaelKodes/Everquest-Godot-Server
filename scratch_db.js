const mysql = require('mysql2/promise'); async function test() { const pool = mysql.createPool({ host: '127.0.0.1', user: 'peq', password: 'peq', database: 'peq' }); const [rows] = await pool.query('SELECT name, opentype FROM doors WHERE zone=\'gfaydark\''); console.log(rows.filter(r => r.name.toLowerCase().includes('ele') || r.name.toLowerCase().includes('lift') || r.name.toLowerCase().includes('step') || r.name.toLowerCase().includes('plat') || r.name.toLowerCase().includes('fay'))); process.exit(0); } test();
